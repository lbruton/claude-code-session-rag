# SessionFlow Project Agent Assets

This directory is reserved for Codex-compatible mirrors of project-local Claude assets.

Current parity check:

- `.claude/project.json` exists and is Claude project metadata, not a skill/command/agent asset.
- No `.claude/skills/`, `.claude/commands/`, or `.claude/agents/` directories currently exist.
- Add mirrored Codex-compatible assets here if project-local Claude skills, commands, or agents are introduced later.
